apiVersion: v1
kind: Service
metadata:
  name: frontend-app-service
  labels:
    app: frontend-app
spec:
  ports:
    - name: "3000"
      port: 3000
      targetPort: 3000
  selector:
    app: frontend-app
  type: LoadBalancer
